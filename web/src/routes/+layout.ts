// FaroOS's UI is a pure client-side SPA served as static files by the Go
// server — no SSR, no prerendering (routes are dynamic: node IDs, etc.).
export const ssr = false;
export const prerender = false;
